import React from 'react';

// Placeholder for future muzzle flash / hit particle VFX
// Currently post-processing Bloom handles the glow on bullets and loot
const VFX3D: React.FC = () => {
  return null;
};

export default React.memo(VFX3D);
